import React from 'react';
import Try from './Try';
const Home = () => (
  <div>
    <h1> Home... changing things just a little bit! </h1>
    <p>Currently using React {React.version}</p>
    {/* <Try /> */}
  </div>
);

export default Home;
